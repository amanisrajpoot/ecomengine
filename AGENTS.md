# Agents

Do not scan the repository. Route through:

1. `docs/agent-context/INDEX.md`
2. `docs/agent-context/STATE.md`
3. `PHASES.md` **or** `ROUTES.md` **or** `MAP.md` (one of them)
4. Register new files in `MAP.md` in the same PR

`SCHEMA.md` = line ranges into `docs/schema.md` (never read the whole spec).
