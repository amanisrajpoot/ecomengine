# Agents

Compact context for coding (avoid loading all of `docs/`):

1. `docs/agent-context/INDEX.md`
2. `docs/agent-context/STATE.md`
3. Only the INDEX row for the task
4. Update `STATE.md` in the same PR when the engine moves forward

Protocol: `docs/agent-context/PROTOCOL.md`
